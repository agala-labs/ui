# Internal playground

The original all-components playground remains available as a maintainer stress-test surface. It intentionally favors broad state coverage over consumer-facing documentation.

```bash
npm run playground:dev
```

The public documentation examples are smaller, source-grounded, and organized by package. Use the playground when checking cross-component theme changes or regression behavior across the whole library.
